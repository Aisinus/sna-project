syntax = "proto3";

service ClientMetricService {
    rpc SendMetric(Metric) returns (Empty);
}

message Metric{
    float cpu = 1;
    float ram = 2;
    int64 timestamp = 3;
    string ip = 4;
}

message Empty{}
